---
import { Image } from 'astro:assets';
import MiPerfil from '../assets/images/imagen-perfil-temp.png';
---
<div class="hero-particles" id="particles-container"></div>
    <div class="container mx-auto px-4">
        <div class="flex flex-col md:flex-row items-center justify-between gap-12">
            <div class="md:w-1/2" data-aos="fade-right" data-aos-duration="1000">
                <h1 class="text-4xl md:text-6xl font-bold mb-4">
                    <span class="block">Desarrollador</span>
                    <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-500">FullStack</span>
                </h1>
                <p class="text-lg text-gray-300 mb-8">Apasionado por asumir nuevos retos y estar en constante aprendizaje. Me motiva explorar nuevas tecnologías, mejorar mis habilidades cada día y crear soluciones que generen impacto.</p>
                <a href="/cv/Hoja-de-vida-santiago.pdf" target="_blank" rel="noopener noreferrer" class="neon-button">Ver cv</a>
            </div>
            <div class="md:w-1/2 relative" data-aos="fade-left" data-aos-duration="1000">
                <div class="relative w-64 h-64 md:w-96 md:h-96 mx-auto">
                    <div class="absolute inset-0 rounded-full bg-gradient-to-r from-blue-500 to-purple-500 opacity-20 blur-xl"></div>
                    <Image src= { MiPerfil } alt="mi-perfil" class="rounded-full w-full h-full object-cover relative z-10 border-4 border-gray-800" />
                </div>
            </div>
    </div>
</div>

<script is:inline>
        const particlesContainer = document.getElementById('particles-container');
            const particleCount = 50;

            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.classList.add('particle');
                
                // Random position
                const x = Math.random() * 100;
                const y = Math.random() * 100;
                
                // Random size
                const size = Math.random() * 2 + 1;
                
                // Apply styles
                particle.style.left = `${x}%`;
                particle.style.top = `${y}%`;
                particle.style.width = `${size}px`;
                particle.style.height = `${size}px`;
                
                // Random opacity
                particle.style.opacity = Math.random() * 0.5 + 0.3;
                
                // Random color (blue or purple tint)
                const colors = ['rgba(0, 242, 255, 0.8)', 'rgba(153, 0, 255, 0.8)', 'rgba(255, 255, 255, 0.8)'];
                const color = colors[Math.floor(Math.random() * colors.length)];
                particle.style.backgroundColor = color;
                
                // Random animation
                const duration = Math.random() * 20 + 10;
                particle.style.animation = `float ${duration}s infinite ease-in-out`;
                
                // Add keyframe animation dynamically
                const style = document.createElement('style');
                const animName = `float-${i}`;
                style.textContent = `
                    @keyframes ${animName} {
                        0%, 100% { transform: translate(0, 0); }
                        50% { transform: translate(${Math.random() * 30 - 15}px, ${Math.random() * 30 - 15}px); }
                    }
                `;
                document.head.appendChild(style);
                
                particle.style.animation = `${animName} ${duration}s infinite ease-in-out`;
                
                particlesContainer.appendChild(particle);
            }
    </script>